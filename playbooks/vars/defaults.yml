---
###############################################################################
## DO NOT EDIT. Set your variables in `vars/user.yml` instead.
## This is a reference of all the variables.
###############################################################################

# UNIX users
unix_users:
  - root

# Default UNIX password is empty
default_unix_password: $1$SomeSalt$sLtuObRBEqqpW3Uc0aRyb1

# ntp
ntp_servers:
  # use nearby ntp servers by default
  - 0.pool.ntp.org
  - 1.pool.ntp.org
  - 2.pool.ntp.org
  - 3.pool.ntp.org
